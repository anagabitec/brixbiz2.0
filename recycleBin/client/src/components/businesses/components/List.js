import React from "react";

function List(props) {
    return (
        <ul className="list-group">
            <li className="type-list"> key={typeof.id}>
                <img src={type.img}></img>
                {type.type}
            </li>
        </ul>
    );
}

export default List;